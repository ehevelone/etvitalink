// Root build.gradle for modern Flutter projects
// Plugin management is now in settings.gradle

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

tasks.register("clean", Delete) {
    delete rootProject.layout.buildDirectory.get()
}
