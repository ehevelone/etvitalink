[build]
  functions = "functions"
  publish = "public"
  command = "echo 'No frontend build step needed'"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["pg", "nodemailer"]

[build.environment]
  NODE_VERSION = "18"
  SMTP_HOST = "smtp.gmail.com"
  SMTP_PORT = "587"
  SMTP_USER = "ehevelone@gmail.com"
  # 🚨 SMTP_PASS stays in Netlify EVs, not here.

# 🔔 Test schedule: every 2 minutes (temporary so we can see it fire)
[[scheduled.functions]]
  name = "weeklyReport"
  cron = "*/2 * * * *"

# 🔔 Production schedule: 6 AM Central (11 AM UTC) daily
[[scheduled.functions]]
  name = "weeklyReport"
  cron = "0 11 * * *"
