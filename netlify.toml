[build]
  # No site to build — this is a functions-only deployment
  publish = "functions"
  command = ""

[functions]
  directory = "functions"
  node_bundler = "esbuild"
  included_files = ["functions/**"]
