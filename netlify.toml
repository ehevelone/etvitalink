[build]
  publish = "build/web"

[functions]
  directory = "functions"
  node_bundler = "esbuild"
  external_node_modules = ["pg", "bcryptjs"]

# 🚨 this prevents tree-shaking from removing db.js
included_files = ["functions/services/**/*.js"]
