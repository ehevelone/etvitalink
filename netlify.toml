[build]
  functions = "functions"
  publish = "public"
  command = "echo 'No frontend build step needed'"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["pg", "nodemailer", "qrcode"]

[build.environment]
  NODE_VERSION = "18"
  SMTP_HOST = "smtp.gmail.com"
  SMTP_PORT = "587"
  SMTP_USER = "ehevelone@gmail.com"
  # 🚨 SMTP_PASS stays in Netlify EVs, not here.
